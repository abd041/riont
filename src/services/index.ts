/**
 * Client-side service layer (browser-only helpers, API wrappers).
 *
 * Server data access lives in src/server/services/ (RSC + Server Actions).
 * Feature-specific orchestration belongs in features/[name]/services/ when needed.
 *
 * Add modules here only for cross-feature client utilities (e.g. analytics, search cache).
 */

export {};
